{% extends 'base.html.twig' %}

{% block content %}
    <div class="row">
        <div class="col-xs-12">
            {{ form_start(form, { 'action': path('admin_register') }) }}
                {{ form_widget(form) }}

                <button type="submit" class="btn btn-default">Submit</button>
            {{ form_end(form) }}
        </div>
    </div>
{% endblock %}